const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'src', 'config', 'database.js'), //yarn sequelize db:create
    'migrations-path': path.resolve(__dirname, 'src', 'database', 'migrations'), //yarn sequelize migration:create --name=create-nomedatabela
}