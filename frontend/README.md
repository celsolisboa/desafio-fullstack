# Frontend

`cd frontend`,Basta rodar o comando `npm install`, para instalar as dependencias. 
 Após a instalação das mesmas  e dentro da pasta frontend rodar o comando `npm run start` no terminal, para inicializar o servidor do angular.
